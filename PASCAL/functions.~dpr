library functions;


uses
  SysUtils,
  Classes,
  mod_f in 'mod_f.pas',
  mid_f in 'mid_f.pas',
  compareStrings_f in 'compareStrings_f.pas',
  factorial_f in 'factorial_f.pas',
  f2c_f in 'f2c_f.pas',
  pv_f in 'pv_f.pas',
  c2f_f in 'c2f_f.pas',
  dec2hex_f in 'dec2hex_f.pas',
  hex2dec_f in 'hex2dec_f.pas',
  dec2bin_f in 'dec2bin_f.pas',
  trim_f in 'trim_f.pas';

{$R *.res}
    exports Modulo,MID,compare,factorial,f2c,PV,c2f,dec2hex,hex2dec,dec2bin,trimc;
begin
end.
